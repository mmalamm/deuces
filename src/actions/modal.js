export const showNewGameForm = () => dispatch => {
  dispatch({ type: "SHOW_NEW_GAME_FORM" });
};
export const hideModal = () => dispatch => {
  dispatch({ type: "HIDE_MODAL" });
};
export const showChangePicForm = () => dispatch => {
  dispatch({ type: "SHOW_CHANGE_PIC_FORM" });
};
